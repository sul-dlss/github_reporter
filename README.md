# github_reporter
Query the github API

## How to use the reporter
[Generate an access token](https://help.github.com/articles/creating-an-access-token-for-command-line-use/) and plug it into `config.yml`. Then, you can run scripts in `bin`, or write your own methods/specs for custom scripts.
